'use client';

// This component is temporarily disabled due to a package installation issue with its dependency.
// It will be re-enabled once a suitable library is found.
export function EthiopianCalendar() {
  return null;
}
